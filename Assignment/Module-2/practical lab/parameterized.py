# Function to add two numbers
def add_numbers(a, b):
    result = a + b
    print("The sum of", a, "and", b, "is:", result)

# Calling the function with arguments
add_numbers(10, 20)
add_numbers(5, 7)
